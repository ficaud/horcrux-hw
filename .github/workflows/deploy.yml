name: Deploy docs to GitHub Pages

on:
  push:
    branches:
      - main
      - dev/jfi
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Configure git
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com

      - name: Deploy with mike
        run: |
          # Reset the gh-pages branch entirely to clear any stale/corrupted
          # version state (e.g. "latest" wrongly registered as a version).
          git push origin --delete gh-pages || true
          if [ "${{ github.ref_type }}" = "tag" ]; then
            VERSION="${GITHUB_REF_NAME#v}"
            mike deploy "$VERSION" latest --push --update-aliases
            mike set-default latest --push
          else
            mike deploy dev --push --update-aliases
            mike set-default dev --push
          fi

